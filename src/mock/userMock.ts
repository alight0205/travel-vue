import type {userRes} from '@/types/user'
export const userListMock = <userRes[]>[
    {
        id: 1,        // 用户 ID（必填）
        username:"ceshi1",  // 用户名（可选）
        password: "ceshi1",  // 密码（可选）
        nickname: "测试1", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试1的简介",     // 个人描述（可选）
        email: "ceshi1@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 2,        // 用户 ID（必填）
        username:"ceshi2",  // 用户名（可选）
        password: "ceshi2",  // 密码（可选）
        nickname: "测试2", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试2的简介",     // 个人描述（可选）
        email: "ceshi2@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 3,        // 用户 ID（必填）
        username:"ceshi3",  // 用户名（可选）
        password: "ceshi3",  // 密码（可选）
        nickname: "测试3", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试3的简介",     // 个人描述（可选）
        email: "ceshi3@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 4,        // 用户 ID（必填）
        username:"ceshi4",  // 用户名（可选）
        password: "ceshi4",  // 密码（可选）
        nickname: "测试4", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试4的简介",     // 个人描述（可选）
        email: "ceshi4@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 5,        // 用户 ID（必填）
        username:"ceshi5",  // 用户名（可选）
        password: "ceshi5",  // 密码（可选）
        nickname: "测试5", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试5的简介",     // 个人描述（可选）
        email: "ceshi5@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 6,        // 用户 ID（必填）
        username:"ceshi6",  // 用户名（可选）
        password: "ceshi6",  // 密码（可选）
        nickname: "测试6", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试6的简介",     // 个人描述（可选）
        email: "ceshi6@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 0// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 7,        // 用户 ID（必填）
        username:"ceshi7",  // 用户名（可选）
        password: "ceshi7",  // 密码（可选）
        nickname: "测试7", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试7的简介",     // 个人描述（可选）
        email: "ceshi7@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 0// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 8,        // 用户 ID（必填）
        username:"ceshi8",  // 用户名（可选）
        password: "ceshi8",  // 密码（可选）
        nickname: "测试8", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试8的简介",     // 个人描述（可选）
        email: "ceshi8@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 9,        // 用户 ID（必填）
        username:"ceshi9",  // 用户名（可选）
        password: "ceshi9",  // 密码（可选）
        nickname: "测试9", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试9的简介",     // 个人描述（可选）
        email: "ceshi9@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 0// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 10,        // 用户 ID（必填）
        username:"ceshi10",  // 用户名（可选）
        password: "ceshi10",  // 密码（可选）
        nickname: "测试10", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试10的简介",     // 个人描述（可选）
        email: "ceshi10@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },
    {
        id: 11,        // 用户 ID（必填）
        username:"ceshi11",  // 用户名（可选）
        password: "ceshi11",  // 密码（可选）
        nickname: "测试11", // 昵称（可选）
        avatar: "",   // 头像 URL（可选）
        desc: "测试11的简介",     // 个人描述（可选）
        email: "ceshi11@test.com",    // 邮箱地址（可选）
        created_at: "1546926630", // 账号创建时间（可选）
        updated_at: "1546926630", // 更新时间（可选）
        examine: 1// 审核状态（0-未通过，1-已通过，默认为1）
    },

]

